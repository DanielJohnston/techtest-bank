source "https://rubygems.org"
ruby ">= 2.2.0"

group :test, :development do
  gem 'rspec'
  gem 'capybara'
  # This is used to avoid requiring ruby 2.4.0, which now has binding.irb built in
  gem 'pry'
end
